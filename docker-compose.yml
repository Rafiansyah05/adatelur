services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    container_name: adatelur-web
    restart: always
    ports:
      - "3020:3000"
    env_file:
      - .env
